{
  "name": "ocx-dash",
  "version": "0.1.0",
  "private": true,
  "description": "OpenCodex 增强仪表盘 — 谷歌反重力额度可视化面板",
  "license": "MIT",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "dev:web": "vite",
    "build:web": "vite build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
